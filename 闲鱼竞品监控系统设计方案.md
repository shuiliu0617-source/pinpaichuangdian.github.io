# 闲鱼竞品监控系统设计方案（V1）

## 1. 目标与边界

### 1.1 业务目标
- 自动追踪竞品在闲鱼上的价格、销量代理指标（想要人数/留言活跃度）、上新节奏、文案卖点与服务承诺变化。
- 对异常变化进行告警：如短时间降价、爆款快速起量、关键词策略变化。
- 形成周报/月报，为选品、定价、投放与客服策略提供决策依据。

### 1.2 监控对象
- **店铺维度**：重点竞品商家、腰部商家、潜在新进入者。
- **商品维度**：同品类核心 SKU、替代 SKU、高流量低转化 SKU。
- **关键词维度**：品类词、场景词、痛点词、属性词。

### 1.3 合规边界
- 仅采集公开可见信息，避免抓取隐私数据。
- 请求频率严格限流，遵循 robots 与平台服务条款。
- 统一脱敏存储账号/联系方式等敏感字段。

---

## 2. 总体架构

```text
[任务编排层]
  ├─ 定时任务（分钟/小时/天）
  ├─ 事件任务（关键词触发）
  └─ 回填任务（历史补抓）

[采集接入层]
  ├─ 关键词搜索采集器
  ├─ 店铺页采集器
  ├─ 商品详情采集器
  └─ 评论/互动采集器（可选）

[数据处理层]
  ├─ 去重与规范化
  ├─ 实体识别（品牌/型号/规格）
  ├─ 文本标签（卖点/承诺/风险词）
  └─ 指标计算（价格带/活跃度/竞争热度）

[数据存储层]
  ├─ OLTP（MySQL/PostgreSQL）
  ├─ 明细仓库（ClickHouse）
  ├─ 对象存储（原始快照、图片）
  └─ 缓存（Redis）

[分析与应用层]
  ├─ BI 看板（趋势/对比/榜单）
  ├─ 告警中心（飞书/企微/钉钉）
  ├─ 报告生成（周报/月报）
  └─ 策略建议（定价/选品）
```

---

## 3. 核心数据模型

### 3.1 实体表设计（建议）
1. `competitor_shop`
   - `shop_id`、`shop_name`、`level_tag`（头部/腰部/长尾）、`region`
2. `competitor_item`
   - `item_id`、`shop_id`、`title`、`category`、`price`、`want_count`、`publish_time`、`status`
3. `item_snapshot`
   - `item_id`、`snapshot_time`、`price`、`title`、`desc`、`images_hash`、`service_tags`
4. `keyword_rank_snapshot`
   - `keyword`、`snapshot_time`、`rank`、`item_id`、`shop_id`
5. `alert_event`
   - `event_type`、`severity`、`item_id/shop_id`、`trigger_value`、`baseline_value`、`created_at`

### 3.2 指标定义（示例）
- **价格变动率**：`(当前价 - 基线价) / 基线价`
- **上新频率**：近 7 天新增商品数
- **内容迭代频率**：近 7 天标题/描述修改次数
- **竞争热度指数**：关键词排名提升 + 想要人数增速 + 上新速度加权分

---

## 4. 采集策略设计

### 4.1 任务分级
- **L1 高频（15~30 分钟）**：TOP 竞品店铺 + 爆款 SKU。
- **L2 中频（2~4 小时）**：主力关键词搜索页。
- **L3 低频（每日）**：长尾店铺、沉寂商品。

### 4.2 去重与增量
- 使用 `item_id + snapshot_time` 做快照主键。
- 对标题/描述做 hash，未变化时跳过深度处理。
- 失败任务进入重试队列（指数退避）。

### 4.3 反爬与稳定性
- 限流（令牌桶）+ 随机抖动 + 任务错峰。
- IP/UA 池健康度监控，异常自动摘除。
- 采集成功率、平均耗时、封禁率纳入 SLO。

---

## 5. 告警与策略引擎

### 5.1 告警规则（首版）
- 竞品单品 24 小时降价超过 8% → **高优先级告警**。
- 关键词 Top20 新进入 3 个以上新店铺 → **中优先级告警**。
- 某竞品 3 天连续高频上新（>N 件/天）→ **中优先级告警**。

### 5.2 策略建议输出
- **定价建议**：分价位带（低/中/高）给出建议区间。
- **文案建议**：提取竞品高频卖点词（如“全新未拆”“支持验货”）并做正负面归纳。
- **选品建议**：识别“高热度低供给”关键词。

---

## 6. 可视化看板

### 6.1 管理驾驶舱
- 今日告警数、竞品均价、关键词波动、爆款榜。

### 6.2 运营分析页
- 竞品对比（店铺/商品）
- 价格趋势与变动原因回溯
- 文案主题云与卖点演变

### 6.3 周报模板自动化
- 本周新增竞品
- 本周关键价格战事件
- 下周建议动作（调价、上新、文案 A/B）

---

## 7. 技术选型建议（可落地）

- **采集调度**：Airflow / Celery Beat
- **采集执行**：Python（httpx + lxml/bs4 + Playwright 仅用于必要页面）
- **消息队列**：RabbitMQ / Kafka
- **存储**：PostgreSQL + ClickHouse + Redis + MinIO
- **服务框架**：FastAPI
- **可视化**：Metabase / Superset / 自研 Vue 面板
- **告警通知**：企业微信/飞书机器人 Webhook

---

## 8. 分阶段实施计划

### Phase 1（2~3 周）MVP
- 完成关键词与商品详情采集链路。
- 打通快照入库、基础指标、基础告警。
- 输出第一个可用看板（价格与上新）。

### Phase 2（3~5 周）增强
- 引入文本标签模型，支持卖点归因。
- 增加店铺健康度评分与竞品分层。
- 上线周报自动生成。

### Phase 3（持续迭代）智能化
- 引入异常检测模型（时序）减少误报。
- 引入策略推荐（定价/选品）闭环评估。

---

## 9. 风险与应对

- **平台策略变化**：解析器版本化 + 回归测试样本库。
- **数据噪声高**：多源交叉验证 + 异常值清洗。
- **误报影响运营**：设置“观察期规则”和人工确认流。
- **成本失控**：按层级任务降频，热点资源优先。

---

## 10. 你可以直接用的最小版本（建议）

如果你要“先跑起来”，建议先做这 4 个功能：
1. 维护 50 个竞品 SKU 列表。
2. 每小时抓一次价格 + 想要人数 + 标题。
3. 24 小时降价超过 8% 自动推送到飞书。
4. 每周一自动生成竞品变化周报。

这个最小闭环通常 1~2 周即可看到业务价值。
